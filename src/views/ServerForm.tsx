import * as React from 'react';

export const ServerForm = () => (
    <div>
        <form>
            Server form
            <div>
                <button>Cancel</button>
            </div>
        </form>
    </div>
);